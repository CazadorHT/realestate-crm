# AI Pair Programming Rules & Guidelines (realestate-crm)

ไฟล์นี้คือข้อตกลงในการทำงานร่วมกับ AI เพื่อให้ได้งานคุณภาพสูง ประหยัด Token และปลอดภัยตรงตามมาตรฐานของโปรเจกต์

## 1. Core Principles & Safety
- **High Type Safety**: ห้ามใช้ `any` เด็ดขาด ทุกอย่างต้องมี Type/Interface รองรับ และ Map กับ `database.types.ts` ให้ถูกต้อง
- **Error Handling**: ทุก Action/Query ต้องมีการดัก Error (try-catch หรือเช็ค `error` จาก Supabase) และแสดงผล Error Message ที่ User เข้าใจ (ภาษาไทย)
- **User Error Prevention**: ตรวจสอบความถูกต้องของข้อมูล (Validation) ก่อนลงมือทำ และป้องกันการกดซ้ำ (Loading States)
- **Deletion Safety**: ก่อนจบงาน AI ต้อง "รีเช็ค" โค้ดรอบข้างเสมอว่าเผลอลบอะไรที่ไม่เกี่ยวข้องทิ้งไปหรือไม่

## 2. Audit Logging (Critical)
- **Every Mutation**: ทุกการกระทำที่เปลี่ยนสถานะข้อมูล (Create, Update, Delete, Trash, Restore) **ต้อง** เรียกใช้ฟังก์ชัน `logAudit` จาก `@/lib/audit.ts` เสมอ
- **Proper Action Enum**: เลือกใช้ `AuditAction` ให้ตรงกับสิ่งที่ทำ (ดูได้ใน `lib/audit.ts`)

## 3. Tech Stack & UX
- **Styling**: ใช้ **Tailwind CSS** เท่านั้น เน้นความ Premium (Gradients, Glassmorphism, Smooth Transitions)
- **Localization**: ข้อความที่แสดงผลบนหน้าจอ (UI/Labels/Messages) ต้องเป็น **ภาษาไทย** ทั้งหมด
- **Lucide Icons**: ใช้ Icons จาก `lucide-react` เพื่อความสวยงามและสม่ำเสมอ

## 4. Quota & Token Saving (Efficiency)
- **Be Concise**: ตอบให้สั้น กระชับ ตรงจุด เน้นโค้ดที่เปลี่ยน ไม่ต้องร่ายยาวถ้าไม่จำเป็น
- **No Placeholders**: อย่าใช้ TODO หรือ Placeholder ถ้ามีข้อมูลครบให้เขียนโค้ดจริงทันที
- **Check Metadata First**: ใช้ข้อมูลจาก Open Tabs และ Cursor Position ที่ระบบส่งมาให้ เพื่อลดการถามซ้ำซ้อน

## 5. Implementation Workflow
1. **Research**: อ่านไฟล์ที่เกี่ยวข้องให้ครบก่อนเริ่ม
2. **Plan**: สรุปขั้นตอนสั้นๆ ให้ User ตรวจสอบ
3. **Audit Check**: มั่นใจว่ามีการใส่ Audit Log แล้ว
4. **Final Review**: ตรวจสอบการลบไฟล์/บรรทัดที่เกินมา (Safety Sweep)
