# 🛡️ Master Forensic Audit Summary & Hardening Roadmap (Enterprise Grade)

### 🟥 Tier 0: Critical Security & Integrity (High Priority - แก้ไขทันที)
| ลำดับ | หัวข้อความเสี่ยง | วิธีแก้ไขที่ถูกต้อง (The Right Way) | ไอเดียเสริม (Idea Bonus) |
| :--- | :--- | :--- | :--- |
| 1 | **PDPA Public Leak**: หน้า Search และหน้าบ้านแอบส่ง `select(*)` ทำให้เบอร์เจ้าของและค่าคอมฯ รั่วไหล | **Restrictive Projection**: เขียนชุดคำสั่ง `BOT_SELECT_FIELDS` ให้ครอบคลุมทุก Public Query ห้าใช้ `*` เด็ดขาด | **Data Masking**: ทำฟังก์ชัน SQL ให้ Return แค่ 4 ตัวท้ายของเบอร์โทรศัพท์ในฝั่ง Public |
| 2 | **Auth Bypass**: มีโค้ด Mock Supabase หลุดอยู่ใน `authz.ts` เสี่ยงต่อการโดนบุกรุก | **Purify Auth Paths**: ถอนเครื่องมือทดสอบออกจาก Production Code และใช้ Environment Variable คุมเข้ม | **Sentinel Logging**: แจ้งเตือนทันทีเมื่อมีการพยายามเรียกใช้ Mock ใน Prod |
| 3 | **Data Ghost Records**: ลบทรัพย์สินแล้วแต่ "กฎการแจ้งเตือน" และ "ประวัติการดู" ไม่ถูกลบตาม | **Cascading Delete / DB Triggers**: ใช้พลังของ Postgres Trigger ในการทำความสะอาดขยะหลังบ้านแบบ 100% | **Cold Storage**: ย้ายข้อมูลสำคัญที่ถูกลบไปลง Archive Table แทนการลบทิ้ง |
| 4 | **Branch Isolation Leak**: หลายจุดใช้ Admin Client จนข้ามระบบความปลอดภัย RLS | **RLS Policy Enforcement**: เลิกใช้ Service Role ใน Server Actions และเขียน Policy ให้ฉลาดขึ้น | **Safe-Proxy Client**: ทำตัวครอบ Client ที่จะกรอก `tenant_id` ให้อัตโนมัติในทุกคำสั่ง |
| 5 | **Plaintext Secrets**: API Tokens (Meta/LINE) เก็บเป็นข้อความธรรมดาใน DB | **Secret Encryption**: ใช้ AES-256 เข้ารหัส Token ก่อนบันทึกลงฐานข้อมูลในระดับ Application | **Key Rotation**: ระบบเปลี่ยน Key อัตโนมัติทุก 90 วัน |
| 6 | **Shadow Ban (AI Sync)**: AI ล่มตอนทำ Embedding ทำให้ทรัพย์ล่องหนจากการค้นหา | **Resilience Retries**: ใช้ Inngest Retry logic และ Status Flag `indexed_at` เพื่อตรวจสอบความสมบูรณ์ | **Dead-letter Queue**: แยกทรัพย์ที่ Index ไม่สำเร็จมาให้ Admin ตรวจสอบ |
| 7 | **Model Integrity**: ข้อมูล `author` ใน Blog ซ้ำซ้อนกับ `author_id` (เสี่ยง Stale Data) | **Database Normalization**: ลบ JSONB แฝง และใช้ Foreign Key Joins จริง 100% | **Strict Schema Enforcement**: ใช้ Zod บังคับความถูกต้องของโมเดลทุกจุด |

---

### 🟧 Tier 1: Performance & Scalability (รองรับการขยายตัว)
| ลำดับ | หัวข้อความเสี่ยง | วิธีแก้ไขที่ถูกต้อง (The Right Way) | ไอเดียเสริม (Idea Bonus) |
| :--- | :--- | :--- | :--- |
| 8 | **Auth Waterfall**: ทุกการคลิกต้องรัน Auth 5 รอบซ้อน ทำให้เว็บหน่วง 0.5s | **Request-Memoization**: ใช้ `React.cache()` ครอบฟังก์ชัน Auth และ SiteSettings ยิง DB ครั้งเดียวต่อ 1 Request | **Session Pre-fetch**: เก็บสิทธิการใช้งานพื้นฐานไว้ใน JWT Metadata |
| 9 | **Middleware Latency**: หน้า Public ต้องรอเช็ค Session/Redis เรียงลำดับกัน | **Early Exit / Parallelize**: แยก Public Routes ออกจาก Auth middleware และรัน Redis/Auth แบบขนาน | **Edge Config**: เก็บ Whitelist ไว้ที่ Edge เพื่อความเร็วระดับมิลลิวินาที |
| 10 | **O(N) Data Aggregation**: ดึงข้อมูลหมื่นแถวมารวมเลขใน JS (ดึงหนัก-ประมวลผลช้า) | **SQL Server-side Aggregation**: ใช้ `GROUP BY` และ `SUM/COUNT` ใน Database ให้เสร็จ | **Materialized Views**: ทำตารางสรุปผลลัพธ์ล่วงหน้าสำหรับ Dashboard |
| 11 | **Rendering Storm**: Prop Drilling ในหน้า Search ทำให้พิมพ์ 1 ตัวสั่นทั้งหน้า | **Atomic State (Zustand)**: ย้าย State ไปไว้ใน Store กลาง เพื่อให้ Rerender เฉพาะจุดที่เปลี่ยน | **Selector Optimization**: ใช้ Zustand Selectors เพื่อป้องกัน UI กระพริบ |

---

### 🟩 Tier 2: AI Cost & Intelligence (ลดต้นทุนและเพิ่มความฉลาด)
| ลำดับ | หัวข้อความเสี่ยง | วิธีแก้ไขที่ถูกต้อง (The Right Way) | ไอเดียเสริม (Idea Bonus) |
| :--- | :--- | :--- | :--- |
| 12 | **AI Vision Token Waste**: ส่งรูปขยะไปให้ AI วิเคราะห์เสียเงินฟรี | **Heuristic Visual Check**: เช็คขนาด/Metadata ก่อนส่ง และจำกัดเฉพาะรูปปก 1-3 รูป | **Smart Throttling**: วิเคราะห์ซ้ำเฉพาะเมื่อข้อมูลหลักมีการเปลี่ยนแปลง |
| 13 | **Search Context Gap**: AI บรรยายทรัพย์โดยขาดข้อมูลทำเลยอดนิยม | **Context Enrichment**: ดึงข้อมูล Nearby Places (BTS/MRT/Soi) ยัดใส่ Prompt อัตโนมัติ | **Multilingual SEO Pack**: ให้ AI สร้าง Keywords 3 ภาษาหยอดลง Metadata |
| 14 | **SQL Redundant Logic**: ฟังก์ชัน `match_properties` คำนวณ Similarity ซ้ำ 2 รอบ | **Query Optimization**: คำนวณครั้งเดียวใน Subquery หรือ Alias เพื่อลดภาระ CPU ของ DB | **HNSW Index Tuning**: ปรับค่า `m` และ `ef_construction` ให้เหมาะกับปริมาณ RAM |

---

### 🟦 Tier 3: UX & Global Readiness (ความพรีเมียมระดับสากล)
| ลำดับ | หัวข้อความเสี่ยง | วิธีแก้ไขที่ถูกต้อง (The Right Way) | ไอเดียเสริม (Idea Bonus) |
| :--- | :--- | :--- | :--- |
| 15 | **The Language Wall**: หน้าบล็อก/หน้าฉุกเฉิน มีแต่ภาษาไทย | **i18n Global Integration**: ย้ายข้อความ Hardcoded ไปเข้าระบบ `t()` ของ i18next 100% | **Auto-Detection**: โชว์ภาษาตามถิ่นที่อยู่ (IP-based) ของลูกค้า |
| 16 | **Asset Bloat**: ใช้ภาพดิบ ไม่ผ่านการย่อขนาด | **Next/Image Optimization**: บังคับใช้ Image Component เพื่อทำ WebP/AVIF และ Lazy load | **LCP Optimization**: โหลดเฉพาะรูปภาพที่จำเป็นสำหรับหน้าจอนั้นๆ |
| 17 | **Hydration Band-aid**: ใช้ `suppressHydrationWarning` คลุมทั้งเว็บแบบสุ่มเสี่ยง | **Hydration Fix**: แก้ตรรกะเวลา/ภาษาให้ตรงกันระหว่าง Server-Client เพื่อถอน Warning ออก | **Visual Stability**: ใช้ Skeleton Loaders แทนการซ่อน Warning |

---

### 🚀 Tier 4: Future Ecosystem & Hyper-Growth (อนาคตที่ไร้ขีดจำกัด)
| ลำดับ | หัวข้อความทะเยอทะยาน | รายละเอียดการยกระดับ (The Vision) | ผลลัพธ์ทางธุรกิจ |
| :--- | :--- | :--- | :--- |
| 18 | **Fintech Automation (Bank API)** | เชื่อมต่อ API ธนาคารเพื่อตรวจสอบยอดโอนและทำ Payout ให้อัตโนมัติ (ไม่ต้องใช้พนักงานกดมือ) | **Zero-Error Finance**: การเงินแม่นยำ 100% และลดเวลาทำงานบัญชีลง 90% |
| 19 | **AI Market Intelligence** | ใช้ Machine Learning วิเคราะห์ Data ใน Deals เพื่อทำนายรายได้บริษัทล่วงหน้า 3-6 เดือน | **Precision Planning**: รู้ล่วงหน้าว่าเดือนไหนรายได้จะตก เพื่ออัดงบการตลาดได้ทัน |
| 20 | **Proactive AI Agent** | บอทที่ไม่ใช่แค่ "รอตอบ" แต่ "ทักหาลูกค้าเอง" เพื่อ Follow-up ตามพฤติกรรมลูกค้า | **Conversion Booster**: เพิ่มโอกาสปิดดีลด้วยการรุกในจังหวะที่ลูกค้า "ร้อน" ที่สุด |
| 21 | **Marketplace Sync Hub** | ระบบที่ดัน (Push) ข้อมูลทรัพย์ไปขึ้น DDProperty, DotProperty, FB Marketplace ได้ในปุ่มเดียว | **Market Dominance**: ทรัพย์อยู่ทุกที่ในเวลาไม่กี่นาที โดยไม่ต้องลงข้อมูลซ้ำซ้อน |
| 22 | **SRE & Observability** | ติดตั้งระบบ Monitoring (Sentry) และ Disaster Recovery ข้ามโซน (High Availability) | **99.99% Uptime**: สร้างความมั่นใจให้ลูกค้าระดับ Corporate และ Enterprise |

---

### 🏆 Efficiency & Sustainability Strategy

1.  **Strict Typesafety**: สร้าง **"Iron Scoped Client"** DB ที่ถูกบังคับ Type ห้ามใช้ `any` เด็ดขาดเพื่อความปลอดภัยในระยะยาว
2.  **Cost-Effective AI**: ใช้หลักการ **"Analyze Once, Read Many"** วิเคราะห์แล้วบันทึก ห้ามยิง AI ซ้ำหากข้อมูลไม่เปลี่ยน
3.  **Selective Fetching**: เลิกใช้ `select(*)` ในทุกจุด และดึงเฉพาะฟิลด์ที่จำเป็นต่อการแสดงผลเท่านั้น
4.  **Performance First**: รักษาระดับ Interaction Latency ให้ต่ำกว่า **200ms** ตลอดเวลา

---

**บทสรุปสุดท้าย**: 
การตรวจสอบเชิงลึก (Forensic Audit) ได้เสร็จสิ้นสมบูรณ์แล้ว ข้อมูลความเสี่ยงทั้งหมดได้รับการจัดชั้นความสำคัญไว้ใน Blueprint ฉบับนี้ ระบบพร้อมสำหรับการ Hardening เพื่อก้าวสู่การเป็น Enterprise CRM อย่างเต็มตัว

**"จบการพิสูจน์หลักฐาน (End of Forensic Audit)... ระบบ CRM ของคุณ Hunter พร้อมสำหรับการศัลยกรรมแล้วครับ!"** 🧪💎🔥🚀🛡️