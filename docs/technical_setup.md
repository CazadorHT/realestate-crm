# 🛠️ Technical Setup Guide (คู่มือการติดตั้งทางเทคนิค)

คู่มือนี้สำหรับนักพัฒนาหรือผู้ดูแลระบบที่ต้องการติดตั้งและใช้งานโปรเจค Real Estate CRM

---

## 1. ความต้องการของระบบ (Prerequisites)

- **Node.js:** 20.x หรือสูงกว่า
- **Package Manager:** npm (แนะนำ) หรือ pnpm
- **Supabase Account:** สำหรับฐานข้อมูลและระบบสมาชิก
- **Google AI Studio:** สำหรับ GEMINI API Key
- **LINE Developers:** สำหรับ Messaging API Channel

---

## 2. การตั้งค่าตัวแปรสภาพแวดล้อม (Environment Variables)

สร้างไฟล์ `.env.local` ใน Root Directory และระบุค่าต่อไปนี้:

### 🌐 Supabase Configuration

- `NEXT_PUBLIC_SUPABASE_URL`: URL ของโปรเจค Supabase
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`: Anon/Publishable Key สำหรับ Client-side
- `SUPABASE_SERVICE_ROLE_KEY`: Service Role Key สำหรับระบบหลังบ้าน (ห้ามเปิดเผย)

### 💬 LINE Integration

- `LINE_CHANNEL_ACCESS_TOKEN`: Access Token จาก LINE Developers Console
- `LINE_CHANNEL_SECRET`: Channel Secret จาก LINE Developers Console

### 🤖 AI Configuration

- `GEMINI_API_KEY`: API Key จาก Google AI Studio เพื่อใช้ระบบ AI Generate

---

## 3. การติดตั้งและรันโปรเจค

1.  **Clone และติดตั้ง Dependency:**
    ```bash
    npm install
    ```
2.  **รันในเครื่อง (Development):**
    ```bash
    npm run dev
    ```
3.  **Build สำหรับใช้งานจริง (Production):**
    ```bash
    npm run build
    npm run start
    ```

---

## 🚀 4. การ Deploy (Deployment)

### Vercel (Frontend & Serverless Functions)

1.  เชื่อมต่อ GitHub Repository กับ Vercel
2.  ตั้งค่า Environment Variables ใน Vercel Dashboard ตามในไฟล์ `.env.local`
3.  Vercel จะทำการ Build และ Deploy โดยอัตโนมัติเมื่อมีการ Push Code

### Supabase (Database & Auth)

1.  ทำการสร้าง Database Tables โดยใช้ Scripts ในโฟลเดอร์ `/supabase` หรือใช้ SQL Editor ใน Supabase Console
2.  ตั้งค่า **Row Level Security (RLS)** ให้ถูกต้องเพื่อความปลอดภัยของข้อมูล

---

## 🔑 5. ระบบระดับสิทธิ์ผู้ใช้ (User Roles)

ระบบประกอบด้วย 3 ระดับสิทธิ์:

1.  **ADMIN:** เข้าถึงได้ทุกส่วนของระบบ รวมถึงการตั้งค่าเว็บไซต์และการจัดการผู้ใช้
2.  **AGENT:** จัดการทรัพย์สิน ดิว และลูกค้าของตัวเองได้
3.  **USER:** ผู้เข้าชมเว็บทั่วไปที่สามารถบันทึกรายการโปรดได้

---
