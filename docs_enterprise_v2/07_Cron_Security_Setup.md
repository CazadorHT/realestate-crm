# ⏱️ 07: ระบบคิวเบื้องหลังและงานรายวัน (Event-Driven & Background Jobs)

> **เครื่องยนต์ประมวลผล:** Inngest, Vercel Cron
> **แนวทางการออกแบบ:** ปล่อยมือจาก Main Thread สู่สถาปัตยกรรมหลุดพ้นจาก Latency 

คุณอาจจะเคยเห็นระบบเว็บพังเวลาสั่งให้สรุปผลรายงาน PDF สิ้นเดือนพร้อมกัน หรือเวลาที่ระบบพยายามโพสต์ Facebook กลับไปยังเพจพร้อมรูปภาพบานตะไท **"ปัญหาสายอืด"** เหล่านี้จะไม่มีในระบบของเราด้วยการปรับรูปแบบเป็น **Event-Driven Architecture**

---

## 1. สถาปัตยกรรม Queue ทะลวงกำแพง Vercel (Inngest Logic)

Vercel มีกฎจำกัด 10 วินาที สำหรับคำสั่ง API ปกติ หากคุณรันสคริปต์ที่ใช้เวลานานโค้ดคุณจะโดนระเบิดทิ้งระหว่างทาง 

### 1.1 Inngest: ท่อส่งงานอัจฉริยะ (The Smart Job Orchestrator)
ทุกครั้งที่เกิดคำสั่งใหญ่ (เช่น ส่งให้ AI เขียนประกาศบ้านภาษาจีน) โค้ดของเราจะ **ไม่รอ** จนกว่าฝั่ง AI จะเขียนเสร็จ แอดมินจึงไม่เจออาการค้าง!
- คำสั่งถูกห่อหุ้มกลายเป็น Event Payload (`ai/property.generate.locale`) และแตะขอบ Inngest อย่างรวดเร็วแล้วโต้ตอบแอดมินทันที "ได้รับคำสั่งแล้ว!" 
- ตัว Inngest Worker จะทำงานเงียบๆ หลังบ้าน คุยกับ Server ของ Google Gemini, รับความเสี่ยงของการเชื่อมต่อล้มเหลว (Automatic Retry) และอัปเดตข้อมูลบ้านลง Database เมื่อทุกอย่างเสร็จสิ้น

### 1.2 "หลอก" Facebook วิ่งลื่นโซเชียล
ระบบ Social Media Studio ដែលตั้งเวลาโพสต์ให้ลูกค้า ก็ไม่ได้ทำงานสดๆ:
เมื่อกดปุ่มยิงโพสต์ โค้ดจะปล่อย Event `social/meta.page.post` ลงคิวทันที หากเกิด Error Facebook ถีบกลับ (เช่น Token ลืมหมุนเวียน หรือขนาดภาพหนักเกิน) 
Inngest จะจัดการส่งการแจ้งเตือนพังทลายเป็น Timeline Error ให้แอดมินมาแก้โดยไม่สะเทือนโครงข่ายเซิร์ฟเวอร์เว็บเลย!

---

## 2. ระบบตื่นนอนทำงานอัตโนมัติ (Cron Jobs Infrastructure)

การเป็น "ผู้ดูแลแทนคน" คือคอนเซปท์ของ Enterprise CRM ไม่ใช่เก็บข้อมูลเฉยๆ แต่ต้อง Action อัตโนมัติ:

### 2.1 Vercel Edge Cron & AI Market Alerts
- **AI Market Drop Alert**: ทุกเช้าตี 3 ระบบจะทำการปิงผ่าน Cron เพื่อดึงราคาปิดดีลทั้งหมดในรัศมีวงกลมเดียวกัน (Radius check) จากนั้นเปรียบเทียบกับบ้านที่อยู่ในสต็อกของเรา หากพบสิ่งผิดปกติว่า "ตลาดแถวพระราม 9 ร่วง" Cron จะตบการกระทำลง Inngest แจ้งเตือนแอดมินแบบฉับพลัน
- **Contract Renewal Notice**: คอยกวาดดูว่า "เดือนหน้าสัญญาคอนโดไหนจะหมด?" ถือเป็นฟีเจอร์แจกเงินเอเยนต์ เพราะ Cron จะสั่งยิงข้อความ Push Flex Message ไปเตือนลูกบ้านทาง LINE 

### 2.2 วงเกราะของ Cron
หน้า Endpoint ของ Cron มีการสวมเกราะรักษาความปลอดภัยด้วย `CRON_SECRET` Header 
ทำให้พวกชอบสแกนเซิร์ฟเวอร์เปิดช่องโหว่ ไม่สามารถมายิง `/api/cron/daily-check` ป่วนระบบเล่นให้อืดได้ หาก Secret Key ไม่ตรงเป๊ะ จะยิงรีเจกเป็นรหัส 401 Unauthorized ขับไล่ออกทันที
